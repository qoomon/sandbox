name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  contents: write
  
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir foo
      - uses: qoomon/sandbox/.github/actions/sandbox@main
          
        
        
