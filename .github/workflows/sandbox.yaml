name: ⚙️ Sandbox
on:
  push:
    branches: ['main']
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        default: 'TEST'

jobs:
  logLatestRelease:
    runs-on: ubuntu-latest
    steps:
      - run: |
          FOO="ab "
          [[ $INPUTS_REPOSITORY = *[[:space:]]* ]] && echo "does contain spaces" || echo "does not contain spaces"


            
            
