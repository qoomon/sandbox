name: ~ Sandbox
on:
  push:
    branches: 
      - main
env:
  scope: MY
  
jobs:
  example:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo $foo
        env:
          FOO: ${{ vars.${{ env.scope}} }}
