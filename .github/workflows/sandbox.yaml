name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sandbox-queue
  queue: max
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: qoomon/actions--oidc-debug@v1
      - parallel:
        - name: Build frontend
          run: echo moin
  
        - name: Build backend
          run: echo moin
  
        - name: Build docs
          run: echo moin


      
