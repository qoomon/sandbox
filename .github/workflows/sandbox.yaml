name: ~ Sandbox
on:
  push:
    branches: ['main']
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        default: 'TEST'

jobs:
  run:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        foo: [1,2,3]
        bar: ['x', 'y']
    steps:
      - run: echo ${{ toJSON(matrix) }}


            
            
