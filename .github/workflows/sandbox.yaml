name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sandbox-queue
  queue: max
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Parallel Execution Block
        parallel:
          steps:
            - run: echo foo
            - run: echo bar


      
