name: ⚙️ Sandbox
on:
  push: 

jobs:
  deployment-example:
    runs-on: ubuntu-latest
    environment: Playground
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          path: './bar'
      - uses: actions/checkout@v4
        with:
          path: './foo'
  
