name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  id-token: write
  
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v8
        env:
          GITHUB_JOB_CHECK_RUNN_ID: ${{ job.check_run_id }}
        with:
          script: |
           console.log('GITHUB_JOB_CHECK_RUN_ID:', process.env['GITHUB_JOB_CHECK_RUN_ID'])

            const jobCheck = await github.rest.checks.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              check_suite_id: process.env['GITHUB_JOB_CHECK_RUN_ID'],
            });
            console.log('jobCheck:', jobCheck)

            
      - uses: qoomon/sandbox/.github/actions/sandbox@main
      - run: |
          env
       

        
        
