name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  contents: write
  
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "::add-mask::2077"
          echo "::set-env name=test_key_id::2077"
          echo ${test_key_id}
          echo 2077

