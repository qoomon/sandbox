name: ⚙️ Sandbox
on:
  push:
    branches: ['main']
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        default: 'TEST'

jobs:
  github-context:
    runs-on: self-hosted
    steps:
      - uses: qoomon/actions--context@v3

            
            
