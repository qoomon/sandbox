name: ⚙️ Sandbox
on:
  push:
    branches: ['main']
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        default: 'TEST'

jobs:
  github-context:
    runs-on: ubuntu-latest
    steps:
      - uses: qoomon/actions--context@main

            
            
