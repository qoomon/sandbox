name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  contents: write
  
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${JOBS}; echo ${JOB};
        env:
          JOB: ${{ toJSON(job) }}
          
        
        
