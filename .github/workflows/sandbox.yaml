name: ⚙️ Sandbox
on:
  push: 
  workflow_dispatch:

permissions:
  contents: write
  
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir foo
      - working-directory: ./foo
        run: |
          pwd
          env | grep GITHUB_
          
        
        
