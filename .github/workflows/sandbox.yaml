name: ~ Sandbox
on:
  push:
    branches: 
      - main
env:
  scope: MY
  
jobs:
  example:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo AWS_ACCOUNT: $AWS_ACCOUNT
        env:
          AWS_ACCOUNT: ${{ vars[format('AWS_ACCOUNT__{0}', env.scope)] }}
