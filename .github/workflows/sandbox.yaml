name: ⚙️ Sandbox
on:
  push: 

jobs:
  deployment-example:
    runs-on: ubuntu-latest
    environment: Playground
    steps:
      - run: |
          echo STEP0=fromStep0 >> $GITHUB_ENV
      - run: |
          echo STEP2=fromStep3 >> $GITHUB_ENV 
      - run: |
          cat $GITHUB_ENV 
