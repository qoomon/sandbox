name: ~ Sandbox
on:
  push:
    branches: 
      - master
  workflow_dispatch:
    inputs:
      buildTimeSeconds:
        default: '10'

permissions:
  id-token: write # This is required for requesting OIDC token
  contents: write
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check if the job should run
        run: |
          if [ "true" == "true" ]; then
            echo "Skipping build job"
            exit 0
          fi
      - name: Build
        run: echo "Building the project..."
            
            
