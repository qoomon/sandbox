name: ⚙️ Sandbox
on:
  pull_request: 

jobs:
  deployment-example:
    runs-on: ubuntu-latest
    environment: Playground
    steps:
      - uses: qoomon/actions--create-comment@main
        with:
          comment: Hello
