name: ~ Sandbox
on:
  push:
    branches: ['main']
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        default: 'TEST'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: export FOO=42
      - run: echo FOO $FOO


            
            
