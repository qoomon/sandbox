name: Manual

on: 
  workflow_dispatch:
  
permissions:
  id-token: write # required to request id-token

jobs:

  run:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            await github.getIDToken()
      

