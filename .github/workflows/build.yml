name: UpdateSecrets
on:
  schedule:
    - cron: '*/15 * * * *'


jobs:
  build:

    runs-on: [ubuntu-latest]
    
    steps:
    - run: npm install
    - name: Rotate Secrets
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: node update-github-repo-secrets.js
  

          

